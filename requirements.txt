# Core (required for Experiments 1, 2, 3)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
requests>=2.28

# Optional (only needed for the optional transformer baseline in Exp.1,
# run with --use_transformer; requires internet access to Hugging Face Hub)
transformers>=4.30
torch>=2.0
